import { SchedulingValidationPipe } from './scheduling-validation.pipe';

describe('SchedulingValidationPipe', () => {
  it('should be defined', () => {
    expect(new SchedulingValidationPipe()).toBeDefined();
  });
});
